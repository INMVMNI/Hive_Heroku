{
  "name": "hivebackend",
  "version": "1.0.0",
  "description": "hive site server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js"
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Tim Perkins",
  "license": "MIT",
  "dependencies": {
    "cloudinary": "^1.22.0",
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "firebase": "^7.24.0",
    "firebase-admin": "^9.2.0",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
